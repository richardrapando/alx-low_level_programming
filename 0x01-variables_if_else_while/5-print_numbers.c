#include <stdio.h>

/**
 * main - Entry point
 *single digit numbers
 * Return: 0 on success
 */

int main(void)
{
	int a = 0;

	for (a = 0; a <=9; a++)
	{
		printf("%d", a);
	}
	printf("\n");

	return (0);

}
